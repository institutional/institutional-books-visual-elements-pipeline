# NOTE: For step 02-04, I think we should consider not storing crops on disk or remote storage at this stage.
# Instead, because we still have access to all the images we need,
# we could write a small utility (maybe models.Detection.crop()) to extract crops from raw scans.
